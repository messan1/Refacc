---
label: Type Page Acceuil
hide_body: false
fields:
- name: en_tete
  type: blocks
  template_types:
  - slide
  config:
    min: 
    max: 
  label: En-tête
- name: a_propos
  type: blocks
  label: A propos
  template_types:
  - a-propos
  config:
    min: 
    max: 
